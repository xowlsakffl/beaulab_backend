# 백엔드 작업 안내

## 확인 순서

- `README.md`는 프로젝트 소개이며, 실제 구현과 계약은 현재 소스가 기준이다.
- `composer.json`, `bootstrap/app.php`, `routes/api.php`와 작업 대상 Actor의 라우트부터 확인한다.
- 해당 도메인의 Request, Action, Query, Model, Policy, DTO와 migration을 함께 읽는다.

## 작업 기준

- `app/Modules`는 Actor별 HTTP 진입점, `app/Domains`는 업무 규칙, `app/Common`은 기술 공통 기능이다.
- 기존 패턴과 API 호환성을 우선한다. 계층 추가나 공통화 자체를 목표로 삼지 않는다.
- 입력 검증과 실행 시점의 권한·소유권·상태 검증을 구분한다. 화면의 버튼 제어를 서버 인가로 대신하지 않는다.
- 금액 변경과 상태 전이는 기존 잠금·멱등성·트랜잭션 경계를 유지한다. 완료된 원장은 직접 수정·삭제하지 않는다.
- 업무 이력과 필요한 발송 대기 기록은 데이터 변경과 함께 보존하고, 외부 전달의 실패·재시도를 구분한다.
- 비밀번호, 인증번호, 토큰, 계좌, 메일 원문 등 민감 정보를 로그나 문서에 기록하지 않는다.
- 기존 미커밋 변경을 보존하고 요청과 무관한 코드·설정·의존성 변경을 피한다.

## 검증과 운영

- 변경 범위에 맞게 PHP 문법, Pint, 기존 테스트를 확인하고 실행하지 못한 검사는 명시한다.
- DB를 사용하는 검사는 연결 대상을 먼저 확인하고 격리된 환경에서만 실행한다.
- DB 초기화, Seeder, 실제 메일·SMS·Push 발송, 큐 재처리, 배포를 점검 목적으로 임의 실행하지 않는다.
- `composer setup`은 키 생성과 migration도 실행한다. 기존 환경에서 설치 명령처럼 사용하지 않는다.

## 문서와 원격 저장소

- `xowlsakffl`에는 `README.md`, `AGENTS.md`만 유지하고 상세 개발·운영 문서는 포함하지 않는다.
- `origin`과 `xowlsakffl`의 README와 문서 구성을 섞지 않는다. 동기화 전후에 커밋의 파일 목록을 확인한다.
- 로컬 절대 경로나 접근할 수 없는 내부 문서에 작업 규칙을 의존시키지 않는다.
