# TODO

이 문서는 아직 구현하지 않은 기능과 운영 후속 작업을 정리한다.

## 신고 기능 후속 정리

- 현재 상태: 토크, 토크 댓글, 병의원 후기, 후기 댓글, 병의원 평가는 신고 생성/신고게시물 관리/상태 처리/경고 처리까지 구현됨.
- 남은 결정: 동일 사용자의 동일 대상 중복 신고를 운영 환경에서도 계속 허용할지 확정 필요. 현재 테스트 편의를 위해 unique 제약과 중복 신고 차단 로직은 비활성화되어 있다.
- 남은 정리: `HospitalEvaluation`에는 기존 `post_status` 컬럼과 필터가 남아 있다. 신규 신고상태는 `ContentReportState.report_status`를 사용하므로 평가 쪽 `post_status` 제거 여부를 결정해야 한다.
- 미구현 범위: 유저 자체 신고, 채팅 메시지 신고는 아직 신고 대상 registry에 포함되지 않았다.

## 채팅방 미디어 정리

- 대상: 채팅 메시지에 연결된 `ChatMessage::MEDIA_COLLECTION_ATTACHMENTS` 미디어만.
- 초안: 보관 기준이 확정되면 30일 지난 채팅 이미지/파일 정리 스케줄을 추가한다.
- 주의: 정상 채팅 메시지의 첨부를 단순 생성일 기준으로 지우면 대화 내 파일 링크가 깨진다.
- 결정 필요 항목: “30일 지난 전체 채팅 첨부 삭제”인지, “삭제/숨김/종료 처리된 채팅 첨부만 30일 후 삭제”인지 정책 확정 필요.
- 현재 상태: TODO만 기록. 코드/스케줄 미구현.
